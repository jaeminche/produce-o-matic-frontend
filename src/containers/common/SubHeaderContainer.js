import React from 'react';
import SubHeader from '../../components/common/SubHeader';
import { menus } from '../../lib/constants/bars';

const SubHeaderContainer = () => {
  return <SubHeader menus={menus} />;
};

export default SubHeaderContainer;
